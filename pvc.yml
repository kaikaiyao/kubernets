apiVersion: v1
kind: PersistentVolumeClaim
metadata:
 name: $PVCNAME
 labels:
   eidf/user: $USER
spec:
 accessModes:
  - ReadWriteMany
 resources:
  requests:
   storage: $STORAGE
 storageClassName: csi-rbd-sc
